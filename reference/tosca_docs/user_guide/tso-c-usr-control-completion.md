# Completion of the Optimization Process

|  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Completion of the Optimization Process | | |  | | | |  | | --- | | This section describes completion of the optimization process.   * Directories and Files after the Completion of Optimization | | |   After the optimization loop has been completed, the directory job name is cleaned up. First, all unnecessary files are deleted. The configuration files determine which files and in what scope (for example, `NEVER`,`FIRST_LAST` or `EVER`) files are saved or deleted. Different decisions for `“wildcards”` can be made in the configuration files (copy, delete etc.). Further details can be found in the specification of the `DRIVER` command.  Directories and Files after the Completion of Optimization The user has a wide range of files for evaluation after optimization has been completed, including the complete model of the initial design and a complete model of the optimized design. Files with additional information are created that serve for the quick evaluation of the optimization. Following subdirectories of the `jobname` directory are created:   * `SAVE.ext`: Contains the original and modified input files and other solver files. * `TOSCA_POST`: Contains the files for postprocessing the optimization results. In addition, the protocol file `TOSCA.OUT` can be found in this directory.   The results of Tosca Structure.SMOOTH are usually saved in this directory as well.  The protocol file `TOSCA.OUT` is copied to the directory `TOSCA_POST` after the optimization has been successfully completed. If the optimization stopped because of an error, the logfile is copied into the directory `DB_ERROR`. |