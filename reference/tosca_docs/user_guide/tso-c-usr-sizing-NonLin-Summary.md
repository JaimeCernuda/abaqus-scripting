# Summary of Guidelines

|  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Summary of Guidelines | | |  | | | |  | | --- | | This section summarizes the guidelines for optimization of structures with nonlinear behavior.   * Check List | | |   Check List The following table contains tips that can be helpful to set up a robust sizing optimization problem.   | Check case: | Check before optimization: | Check after optimization: | | --- | --- | --- | | Are there geometric or material nonlinearities in the model? | The following are considered to be stable settings and are set in:  * Tosca Structure parameter file:    ```   THICKNESS_MOVE   = 0.1   THICKNESS_UPDATE = CONSERVATIVE   ``` * Abaqus input file:    ```   *STEP, NLGEOM: YES, INC=10000, extrapolation=NO   *STATIC   0.1, 1.0, 1e-14, 1.0   ``` | * Check if the results are converged. * If not, look for warnings in the \*.msg and \*.dat files   and use the corresponding command:   + Too many attempts made for this     increment.      ```     *Controls, parameters=time incrementation     , , , , , , , 50, , ,     ```   + Time increment required is less than the     minimum specified      ```     *STEP, NAME=<step name="">, NLGEOM=YES, EXTRAPOLATION=PARABOLIC, INC=1000     ```      Or      ```     *Static, stabilize, factor=0.0002, allsdtol=0.05, continue=NO     ```   + The solution fails to converge in the     maximum number of iterations      ```     *Controls, parameters=time incrementation     , , ,50 , , , , , , ,     ``` * Alternatively, setting `*CONTROLS, ANALYSIS=DISCONTINUOUS` or `*STEP,   UNSYMM=YES` can help to improve convergence. * If the convergence issue remains, impose additional constraints on the displacement or stiffness   of the structure to stabilize the optimization procedure. | | Is there contact interaction in the model? | Make sure that the contact pair is defined by two surfaces with their normal pointing toward each other. | To initialize and stabilize contact interaction, it can be helpful to use the following command:  ``` *CONTACT CONTROL, STABILIZE ``` | | Is there plasticity in the model? | * If possible, use a displacement-controlled load rather   than force-controlled. * The elastic-plastic material card needs have a slight slope after the point of yielding, and   the strain range must cover all strains appearing in the model. | * Check if the results are converged. * If not, look for warnings in the \*msg and \*dat files and   use the corresponding command:   + The strain has exceeded fifty times the     strain to cause first yield at ‘n’ points      ```     *STEP, NAME=<step name="">, NLGEOM=YES, EXTRAPOLATION=PARABOLIC, INC=1000     ```      Or      ```     *Static, stabilize, factor=0.0002, allsdtol=0.05, continue=NO     ``` | |