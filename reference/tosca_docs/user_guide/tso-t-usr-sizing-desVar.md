# Defining Design Variables (DV_SIZING)

|  |  |  |  |  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Defining Design Variables (DV\_SIZING) | | |  | | | |  | | --- | | For each optimization problem, the design variables represent the values to be changed during the optimization. This task shows you how to:   * Design Variable Definition in Tosca Structure.gui * SIMULIA Tosca Structure Parameter File | | |  | | --- | | See Also |  |  | | --- | |  | | In Other Guides | | DV\_SIZING | |   Context:  The elemental thicknesses of shell elements are the design variables for the sizing optimization in SIMULIA Tosca Structure. The thicknesses change during the optimization to fulfill the optimization goals. The following figure shows design variables for sizing:   |  | | --- | |  |   Valid design elements are the most typical shell elements. The list of valid element types for sizing is given in Solver Specific Features.    Design Variable Definition in Tosca Structure.gui 1. Choose Command > DV\_SIZING. 2. Assign a previously defined    Element group to be the design element group for the sizing optimization.    The element group must be a group of elements which are allowed as    design elements for the sizing optimization.  SIMULIA Tosca Structure Parameter File The resulting command is the `DV_SIZING` command: ``` DV_SIZING  ID_NAME  = dv_design_elem  EL_GROUP = design_elem END_ ```  ```  ``` |