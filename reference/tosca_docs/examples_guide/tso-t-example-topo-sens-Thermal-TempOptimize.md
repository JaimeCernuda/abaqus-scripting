# Minimizing Temperature of the CPU Cooler Example

|  |  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Minimizing Temperature of the CPU Cooler Example | | |  | | | |  | | --- | | Shows how to set up a thermal optimization for a CPU cooler with temperature as objective. | | |  | | --- | | See Also | | About Checking the Quality of the Optimization Result | |   1. Define input data and Design Variables: ```    FEM_INPUT      ID_NAME  = MY_INPUT_FILES      FILE     = topo_thermal_cpu_temp.inp    END_     DV_TOPO      ID_NAME  = MY_DV_TOPO      EL_GROUP = ALL_ELEMENTS    END_    ``` 2. To use the temperature in the objective, define a Design Response (DRESP) as follows: ```    DRESP      ID_NAME        = DRESP_Temp      DEF_TYPE       = SYSTEM      TYPE           = TEMPERATURE      ND_GROUP       = MIN_TEMP_NODE    END_    ``` 3. Define the Objective Function (OBJ\_FUNC) as follows: ```    OBJ_FUNC      ID_NAME        = MY_OBJ_FUNC      TARGET         = MIN      DRESP          = DRESP_Temp    END_    ``` 4. Create a constraint for the volume.    1. Define a Design Response with the volume (DRESP): ```       DRESP         ID_NAME        = DRESP_Vol         DEF_TYPE       = SYSTEM         TYPE           = VOLUME         EL_GROUP       = ALL_ELEMENTS       END_       ```    2. Reference the Design Response in a constraint (CONSTRAINT) and constrain it to a maximum of 40%: ```       CONSTRAINT         ID_NAME        = MY_CONSTRAINT_VOL         MAGNITUDE      = REL         DRESP          = DRESP_Vol         LE_VALUE       = 0.40       END_       ``` 5. Define the stamp, symmetry and frozen DVCONs: ```    LINK_TOPO      ID_NAME        = MY_LINK_TOPO_X      CS             = CS_0      TYPE           = PLANE_SYM, AXIS_1    END_     DVCON_TOPO      ID_NAME        = DVCON_PLANESYM_X      CHECK_TYPE     = LINK_TOPO      EL_GROUP       = ALL_ELEMENTS      CHECK_LINK     = MY_LINK_TOPO_X    END_     DVCON_TOPO      ID_NAME        = CASTING      MID_PLANE      = STAMP      CHECK_GROUP    = ALL_ELEMENTS      PULL_DIR       = 0, 1, 0      CHECK_TYPE     = CAST      EL_GROUP       = ALL_ELEMENTS    END_     DVCON_TOPO      ID_NAME        = DVCON_TOPO_Frozen      CHECK_TYPE     = FROZEN      EL_GROUP       = FROZEN_ELEMENTS    END_    ``` 6. Reference the Objective Function, Design Variables and Constraint as well as DVCON in the OPTIMIZE command: ```    OPTIMIZE      ID_NAME        = MY_OPTIMIZATION_TASK      OBJ_FUNC       = MY_OBJ_FUNC      DV             = MY_DV_TOPO      STRATEGY       = TOPO_SENSITIVITY      CONSTRAINT     = MY_CONSTRAINT_VOL      DVCON          = DVCON_TOPO_Frozen      DVCON          = CASTING      DVCON          = DVCON_TOPO_Frozen      DVCON          = DVCON_PLANESYM_X    END_     OPT_PARAM      ID_NAME             = MY_PARAMETERS      OPTIMIZE            = MY_OPTIMIZATION_TASK      TOPO_FILTER_RADIUS  = 5.0    END_    ```      The larger filter radius helps to prevent checkerboarding on this coarse example mesh.  The optimization result looks as follows: |