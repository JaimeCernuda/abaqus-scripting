# Triangular facet shell elements

|  |  |  |  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Triangular facet shell elements | | |  | | | |  | | --- | | Element type STRI3 in Abaqus/Standard is a facet shell—a plate element used to approximate a shell. The element has three nodes, each with six degrees of freedom. The strains are based on thin plate theory, using a small-strain approximation. Arbitrary rigid body rotations are accounted for exactly by formulating the deformation of the element in a local coordinate system that rotates with the element. The element also satisfies the patch test, so that it will produce reliable results with appropriate meshes.  This page discusses:   * [Kinematics](#simathe-c-trifacetshells-t-Kinematics-sma-topic1) * [First variations of strain](#simathe-c-trifacetshells-t-FirstVariationsOfStrain-sma-topic2) * [Second variations of strain](#simathe-c-trifacetshells-t-SecondVariationsOfStrain-sma-topic3) * [Internal virtual work rate](#simathe-c-trifacetshells-t-InternalVirtualWorkRate-sma-topic4) | | |  | | --- | | See Also |  |  | | --- | |  | | In Other Guides | | [About Shell Elements](.._SIMACAEELMRefMap_simaelm-c-shelloverview.md) | |   ProductsAbaqus/Standard  The bending of the element is based on a discrete Kirchhoff approach to plate bending, using Batoz's interpolation functions ([Batoz et al., 1980](simathe-c-references.md#simathe-c-references__simathe-c-ref-batoz)). This formulation satisfies the Kirchhoff constraints all around the boundary of the triangle and provides linear variation of curvature throughout the element. However, the membrane strains are assumed constant within the element. In addition, a curved shell is approximated by this element as a set of facets formed by the planes defined by the three nodes of each element. For these reasons it is necessary to use a reasonably well refined mesh in most applications.    Kinematics A local orthonormal basis system, T1 and T2, is defined in the plane of each element in the reference configuration, using the standard Abaqus convention. S1 and S2 measure distance along T1 and T2 in the reference configuration. Figure 1. Triangular facet shell in the reference configuration.   The membrane strains are then defined as  εα⁢β=12⁢(gα⁢β-Gα⁢β),    α⁢     and⁢    β=1,2  where  gα⁢β=∂x∂Sα⋅∂x∂Sβ  is the metric in the current configuration, and  Gα⁢β=∂⁡X∂⁡Sα⋅∂⁡X∂⁡Sβ  is the metric in the reference configuration.  Here x and X are the spatial coordinates of a point in the current and reference configurations, respectively. Curvature changes are defined incrementally. To account for large rigid body rotations we use a local coordinate system that rotates with the plane defined by the three nodes of the element. The basis vectors chosen for this local system are t1=∂⁡x/∂⁡S1 and t2=∂⁡x/∂⁡S2. Since the membrane strains are assumed to be small, these vectors will be approximately orthonormal. The components of incremental rotation of the normal to the plate are defined as Δ⁢ω1 about t1 and Δ⁢ω2 about t2. The incremental displacement of the reference surface of the plate along the normal to the plane of its nodes is defined as Δ⁢w. (Note that Δ⁢w will be zero at the nodes at all times because the plane containing t1 and t2 always passes through the nodes.) The Kirchhoff constraints are, approximately,  -Δ⁢ω2+∂⁡Δ⁢w∂⁡S1=0  and  Δ⁢ω1+∂⁡Δ⁢w∂⁡S2=0.  [Batoz (1980)](simathe-c-references.md#simathe-c-references__simathe-c-ref-batoz) assumes that Δ⁢ω1 and Δ⁢ω2 vary quadratically over the element and that Δ⁢w is defined independently along each of the three sides of the element as a cubic function. The Kirchhoff constraints are then imposed at the corners and at the middle of each element edge along the direction of the edge to give  Δ⁢ω1=H1P⁢Δ⁢νP  and  Δ⁢ω2=H2P⁢Δ⁢νP,  where Δ⁢νP is the array  ⌊Δ⁢θ11,    Δ⁢θ21,    Δ⁢θ12,    Δ⁢θ22,    Δ⁢θ13,    Δ⁢θ23⌋.  In the above expressions H1P⁢(g,h) and H2P⁢(g,h) are interpolation functions that are defined by [Batoz (1980)](simathe-c-references.md#simathe-c-references__simathe-c-ref-batoz), and the incremental rotation components at the nodes, Δ⁢θαN, are defined as  Δ⁢θαN=nN⋅tα,  where  nN=C⁢(Δ⁢ϕiN)⋅N,  and Δ⁢ϕiN are the increments of the rotational degrees of freedom at the node N, C is the rotation matrix defined by Δ⁢ϕiN, and N is the normal to the plane of the element's nodes at the beginning of the increment. Finally, the incremental curvature change measures are defined as  Δ⁢k11​=∂⁡Δ⁢ω2∂⁡S1Δ⁢k22​=-∂⁡Δ⁢ω1∂⁡S2andΔ⁢k12​=-∂⁡Δ⁢ω1∂⁡S1+∂⁡Δ⁢ω2∂⁡S2.  The three membrane strains and three curvature strains complete the basic kinematic description of the element, except that the use of six degrees of freedom per node introduces a spurious rotation at each node (only two incremental rotations at each node appear in the above equations—the rotation about the normal to the plane of the element's nodes does not enter). To deal with this problem, we define a generalized strain to be penalized with a small stiffness at each node as  γi=b1⋅a1-b2⋅a2,  where  a1​=C⋅A1a2​=C⋅A2A1​=N×A¯1A2​=A¯2×NA¯1​=(Xj-Xi)/|Xj-Xi|A¯2​=(Xk-Xi)/|Xk-Xi|b1​=(xj-xi)/|xj-xi|b2​=(xk-xi)/|xk-xi|  and j, k are the node numbers in cyclic order forming the two sides of the triangle at the node i.  First variations of strain The first variations of strain are  δ⁢εα⁢β​=12⁢(∂⁡x∂⁡Sα⋅∂⁡δ⁢x∂⁡Sβ+∂⁡x∂⁡Sβ⋅∂⁡δ⁢x∂⁡Sα)δ⁢K11​=∂⁡(δ⁢Δ⁢ω2)∂⁡S1δ⁢K22​=-∂⁡(δ⁢Δ⁢ω1)∂⁡S2δ⁢K12​=(∂⁡(δ⁢Δ⁢ω2)∂⁡S2-∂⁡(δ⁢Δ⁢ω1)∂⁡S1),  where  δ⁢Δ⁢ω1​=H1P⁢δ⁢Δ⁢νPδ⁢Δ⁢ω2​=H2P⁢δ⁢Δ⁢νP,  and in Δ⁢δ⁢νP,  δ⁢Δ⁢θαN=δ⁢ϕN×nN⋅tα+nN⋅δ⁢tα.  Also, for the “strain” used to introduce the extra stiffness at the nodes to avoid singularity caused by the component of rotation about the normal,  δ⁢γi=δ⁢b1⋅a1-δ⁢b2⋅a2+b1⋅δ⁢ω×a1-b2⋅δ⁢ω×a2.  Second variations of strain The second variations of strain are  d⁢δ⁢εα⁢β​=12⁢(∂⁡δ⁢x∂⁡Sα⋅∂⁡d⁢x∂⁡Sβ+∂⁡δ⁢x∂⁡Sβ⋅∂⁡d⁢x∂⁡Sα)d⁢δ⁢K11​=∂⁡d⁢δ⁢ω2∂⁡S1d⁢δ⁢K22​=-∂⁡(d⁢δ⁢ω1)∂⁡S2d⁢δ⁢K12​=(∂⁡d⁢δ⁢ω2∂⁡S2-∂⁡d⁢δ⁢ω1∂⁡S1),  where  d⁢Δ⁢δ⁢θαN​=-δ⁢ωN⋅d⁢ωN⁢nN⋅tα+δ⁢ωN⋅nN⁢d⁢ωN⋅tα+δ⁢ωN⋅nN×(∂g1∂S1⁢(d⁢x2-d⁢x1)+∂g2∂S1⁢(d⁢x3-d⁢x1))-(∂g1∂S1⁢(δ⁢x2-δ⁢x1)+∂g2∂S1⁢(δ⁢x3-δ⁢x1))×nN⋅d⁢ωN  and  d⁢δ⁢γi​=δ⁢bi⋅d⁢a1-δ⁢b2⋅d⁢a2+d⁢b1⋅δ⁢ω×a1+b1⋅δ⁢ω×d⁢a1-d⁢b2⋅δ⁢ω×a2-b2⋅δ⁢ω×d⁢a2.  Here g1 and g2 are coordinates in the plane of the element, normalized so that the nodes of the element are at (0,0), (1,0) and (0,1).  Internal virtual work rate The internal virtual work rate is defined as  δ⁢WI=∫A∫hσα⁢β⁢δ⁢εα⁢βf⁢d⁢z⁢d⁢A+∑nodes⁢(i)Ki⁢γi⁢δ⁢γi,  where εα⁢βf=εα⁢β+z⁢kα⁢β is the strain at a point, f, away from the reference surface; σα⁢β are the stress components at f; h is the shell thickness; and Ki is the penalty stiffness used to constrain the spurious rotation.  The formulation now proceeds as for the shell elements described in [Shear flexible small-strain shell elements](simathe-c-shearflexshells.md) using a 3-point integration scheme in the plane of the element. |