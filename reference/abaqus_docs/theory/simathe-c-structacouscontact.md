# Surface-based acoustic-structural medium interaction

|  |  |  |  |  |  |  |  |  |  |  |  |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| |  |  | | --- | --- | | Surface-based acoustic-structural medium interaction | | |  | | | |  | | --- | | Abaqus provides two alternatives for modeling interaction between acoustic and structural media: surface-based interaction or acoustic interface (ASIn) elements. Both are available in Abaqus/Standard, but only the surface-based capability is available in Abaqus/Explicit. If the special-purpose interface elements (ASIn) are used, interacting structural and acoustic nodes must be shared by the two meshes. The surface-based capability can be used for structural and acoustic meshes that have different node numbering and whose surface meshes may not be spatially coincident. The ease of use and low computational cost of the surface-based procedure make it preferable to the element-based approach.  This page discusses:   * [The equations on the contact interface between the structure and the   acoustic medium](#simathe-c-structacouscontact-t-TheEquationsOnTheContactInterfaceBetweenTheStructureAndTheAcousticMedium-sma-topic1) * [Solid/structural main, fluid secondary](#simathe-c-structacouscontact-t-SolidstructuralMasterFluidSlave-sma-topic2) * [Fluid main, solid/structural secondary](#simathe-c-structacouscontact-t-FluidMasterSolidstructuralSlave-sma-topic3) | | |  | | --- | | See Also |  |  | | --- | |  | | In Other Guides | | [Coupled Acoustic-Structural Analysis](.._SIMACAEANLRefMap_simaanl-c-acoustic.md) | |   ProductsAbaqus/StandardAbaqus/Explicit    The equations on the contact interface between the structure and the acoustic medium In the surface-based method the tractions and volumetric acceleration fluxes are computed between structural and acoustic media. In place of consistent distributed tractions or fluxes on both media, one side (identified as the “secondary”) receives point tractions/fluxes based on interpolation with the shape functions from the other (“main”) side. Either the acoustic fluid or the structural solid can be the secondary or main, and no Lagrange multipliers are introduced in the formulation. The basis for deciding which to make secondary or main is discussed in [Coupled Acoustic-Structural Analysis](.._SIMACAEANLRefMap_simaanl-c-acoustic.md#simaanl-c-acoustic).  The transient expressions for the coupled acoustic-structural problem are  ∫Vf[δp(1Kfp¨+rρf⁢Kfp˙)+1ρf∂δ⁢p∂x⋅∂p∂x]dV+∫​Sfs∪Sfrsδpn​-⋅∂p∂xdS-∫​Sftδpa​i⁢ndS+∫Sfr∪Sfrsδ⁢p⁢(rρf⁢1c1⁢p+(rρf⁢1k1+1c1)⁢p˙+1k1⁢p¨)⁢d⁢S=0  (acoustic medium) and  ∫Vδ⁢ε:σ⁢d⁢V+∫Vαc⁢ρ⁢δ⁢um⋅u˙m⁢d⁢V+∫Vρ⁢δ⁢um⋅u¨m⁢d⁢V+∫Sfs∪Sfrsδ⁢um⋅n-⁢p⁢d⁢S-∫Stδ⁢um⋅t⁢d⁢S=0  (structural medium), where n- is the normal vector pointing into the fluid. The fluid-solid surface consists of the union of the directly coupled fluid-solid region, Sfs, and a region coupled via a “reactive” acoustic surface or impedance boundary, Sfrs. Of primary interest here are those terms integrated over Sfs∪Sfrs, which couple the two variational equations. The fluid impedance integral, over Sfr∪Sfrs, depends only on the acoustic pressure field and its variations, so it is unaffected by the contact with the solid. The derivation for the steady-state case is formally identical to the transient case and will not be discussed here. For details of the differences in transient and steady-state acoustics in Abaqus, see [Coupled acoustic-structural medium analysis](simathe-c-acouststruct.md).  When ASIn elements are used (see [Acoustic Interface Elements](.._SIMACAEELMRefMap_simaelm-c-interacoust.md)), the formulation requires that the fluid and solid elements be geometrically and nodally conformal so that the shape functions for the structural displacements and the acoustic pressures are identical. The shape functions are integrated using standard methods to yield element matrices of dimension equal to the number of surface nodes on the element. The complete fluid-solid coupling matrices are formed by the sum over the element faces; that is, a standard element assembly operation. The two final coupling matrices have the sparsity pattern of the coupled fluid-solid element faces.  In surface-based coupling the interaction surface     S fs ∪  S frs  is formed by the boundary between possibly nonconforming structural and acoustic meshes. Therefore, the fluid-solid coupling matrix cannot be broken up into a sum over element faces as simply as in the ASIn case. To derive the coupling matrices in the surface-based procedure, we use a variation of the main-secondary procedure used in small-sliding contact (see [Small-sliding interaction between bodies](simathe-c-smslidcontact.md)). At the start of an analysis, the projections    x N  of secondary nodes onto the main surface are found, and the areas    A N  and normals     n - ⁢  (  x N )  associated with the secondary nodes are computed. The projections are points    p ⁢  (  x N )  on the main surface; main nodes in the vicinity of this projection are identified. Variables at the secondary nodes    x N  are then interpolated from variables at the identified main surface nodes near the projection    p ⁢  (  x N ) .  Since the physical degrees of freedom for the fluid and solid meshes are different, two cases must be treated. The two cases handle the discretization of the coupling terms differently.  Solid/structural main, fluid secondary If the fluid medium surface is designated as the secondary surface, we constrain values at each fluid node to be an average of the values at nearby main surface nodes (see [Figure 1](simathe-c-structacouscontact.md#simathe-c-structacouscontact-t-SolidstructuralMasterFluidSlave-sma-topic2__simathe-c-stmstructacouscontact-slave)). The pointwise fluid-solid coupling condition,  1ρf⁢∂⁡p∂⁡x⋅n-+u¨m⋅n-=0,  is enforced at the secondary nodes, resulting in displacement degrees of freedom added to the fluid secondary surface. These secondary displacements are constrained by the main displacements and thereby eliminated; the secondary pressures are not constrained directly. Figure 1. Fluid secondary surface.   Hence, the fluid equation coupling term  ∫Sfs∪Sfrsδ⁢p⁢n-⋅∂⁡p∂⁡x⁢d⁢S  is equal to  -∫Sfs∪Sfrsδ⁢p⁢n-⋅u¨m⁢d⁢S.  This term is now approximated, at the secondary node level, by the interpolated values of structural displacements at the nearby main nodes times the area of the secondary node:  ∫Sfs∪Sfrsδ⁢p⁢n-⋅u¨m⁢d⁢S≈AN⁢[∑in-⁢(xN)⋅Ni⁢(p⁢(xN))⁢u¨im],  where     N i ⁢  (  p ⁢  (  x N ) )  is the main surface interpolant evaluated at the projection of the secondary node,     u ¨ i m  are the structural accelerations at the main nodes, and     n - ⁢  (  x N )  is the normal vector, pointing into the fluid, evaluated at the secondary node. The summation extends over all main nodes   i  in the vicinity of the secondary node projection    p ⁢  (  x N )  (see [Figure 1](simathe-c-structacouscontact.md#simathe-c-structacouscontact-t-SolidstructuralMasterFluidSlave-sma-topic2__simathe-c-stmstructacouscontact-slave)). The computation is repeated for each secondary node    x N  on the surface     S fs ∪  S frs  and assembled to form the entire coupling matrix.  Similarly, the contribution to the pressure coupling term in the structural equation due to secondary node    x N  is approximated by  ∫Sfs∪Sfrsδ⁢um⋅n-⁢p⁢d⁢S≈pN⁢AN⁢[∑in-⁢(xN)⋅Ni⁢(p⁢(xN))],  where    p N  is the acoustic pressure at secondary node    x N , and the sum is again over the main nodes   i  in the vicinity of the secondary node projection.  These expressions for the coupling terms result in matrices that are the transpose of each other. The normal vectors at the secondary surface are used, so these vectors must be well-defined (see [About Surfaces](.._SIMACAEMODRefMap_simamod-c-surfoverview.md)).  Fluid main, solid/structural secondary If the solid medium is designated as the secondary, the values on this surface are constrained to equal values interpolated from the main surface. The pointwise fluid-solid coupling condition is again enforced at the secondary nodes, resulting in acoustic pressure degrees of freedom added to the solid secondary surface (see [Figure 2](simathe-c-structacouscontact.md#simathe-c-structacouscontact-t-FluidMasterSolidstructuralSlave-sma-topic3__simathe-c-stmstructacouscontact-master)). These secondary pressures are constrained by the main surface acoustic pressures and eliminated; the secondary displacements are not constrained directly. Hence, the contribution to the coupling term in the acoustic equation for a single secondary node    x N  is approximated by  ∫Sfs∪Sfrsδ⁢p⁢n-⋅u¨m⁢d⁢S≈[AN⁢n-⁢(xN)⋅u¨Nm]⁢[∑iHi⁢(p⁢(xN))],  where     u ¨ N m  is the structural acceleration at the secondary node and     H i ⁢  (  p ⁢  (  x N ) )  are the interpolants on the fluid (main) surface evaluated at the projection    p ⁢  (  x N ) . The summation extends over the main nodes   i  in the vicinity of the secondary node projection and is repeated for all solid/structural nodes in the surface     S fs ∪  S frs  to compute the entire coupling matrix. Figure 2. Fluid main surface.   The contribution of a secondary node to the coupling term in the structural equation is approximated by  ∫Sfs∪Sfrsδ⁢um⋅n-⁢p⁢d⁢S≈AN⁢∑iHi⁢(p⁢(xN))⁢pi,  where    p i  is the pressure at main node   i  and the sum is again over the main nodes in the vicinity of the secondary node projection    p ⁢  (  x N ) . |